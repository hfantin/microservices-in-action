server:
  port: 9001

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: True
    service-url:
      default-zone: http://localhost:8761/eureka/
